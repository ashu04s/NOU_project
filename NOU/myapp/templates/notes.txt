 import in url from django.contrib.auth import authenticate,login
 then input field ma set name ="username" then form ka method ma post and 
 

named url 
or class and function based view
template inheritance and compont reuse


how to create model
class Student(models.Model):
    name = models.CharField(max_length=100)
    course = models.CharField(max_length=100)
    branch = models.CharField(max_length=100)
    year = models.CharField(max_length=50)
    image = models.ImageField(upload_to="students/", null=True, blank=True)

    def__str__(self):
        return self.name //object user ka namm sa create hoga

    (primary_key=True  )
